import type { Article } from "@/types/articles";

/**
 * Empty until the Market Insights sprint begins and real articles exist.
 * No placeholder/demo entries — see src/data/projects.ts for why.
 */
export const articles: Article[] = [];
