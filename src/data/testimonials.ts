import type { Testimonial } from "@/types/testimonials";

/**
 * Empty until real client testimonials exist. The public homepage's
 * current placeholder quotes live in /messages, not here — see
 * src/types/testimonials.ts for the migration note.
 */
export const testimonials: Testimonial[] = [];
